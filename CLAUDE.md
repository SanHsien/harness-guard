# 給 AI 助手的安裝指示

你正在讀這份檔案，代表有人把這個 repo 交給你，要你幫他裝起來。

## 先搞清楚你在跟誰講話

**預設對方不會寫程式。** 他很可能只是把一個網址貼給你，然後說「幫我裝」。他不知道什麼是 hook、什麼是 JSON、什麼是環境變數，也不該需要知道。

所以：

- 不要丟術語。真的要用到某個詞，先用一句白話解釋它。
- 不要叫他自己去改設定檔。你改，改完告訴他你改了什麼。
- 不要一次裝完四五樣東西才回報。一次裝一樣，裝完講一句「這個裝好了，它會做什麼」，再問要不要繼續。
- 他說「都好」「你決定」的時候，你就決定，不要再追問細節。他把決定權給你了。

如果對方明顯是工程師（自己講了技術細節、問實作問題），就切換成正常的技術對話，上面這些不適用。

## 裝之前先問三件事

一次問完，不要一題一題來回：

1. **你主要用電腦做什麼？** 寫程式／寫文件跟做筆記／兩者都有。這決定要裝哪幾樣。
2. **有沒有遇過 AI 說「我做好了」但其實沒做的情況？** 有的話 claim-guard 是他最需要的那個。
3. **你在意 AI 在文件裡放表情符號嗎？** 不在意就別裝 no-emoji-guard，省得他之後覺得煩。

## 開始裝之前的檢查

三個 hook 都需要一個叫 `jq` 的小工具（用來讀取資料的）。先確認有沒有：

```bash
which jq
```

沒有的話，Mac 上用這個裝：

```bash
brew install jq
```

如果連 `brew` 都沒有，先別急著叫他去裝 Homebrew，那對非技術使用者是個大工程。先告訴他這件事，問他要不要現在處理，或是先只裝不需要 jq 的東西（skills 那幾個都不需要）。

## 裝的步驟

### hooks（三個攔截工具）

1. 把 `hooks/<工具名>/**claude-code**/` 底下的腳本檔複製到 `~/.claude/hooks/`，**放在該資料夾的最外層，不要保留這裡的子資料夾結構**。注意要拿 `claude-code/` 那份，不要拿到 `codex/` 那份——兩份判斷邏輯一樣但介面寫法不同，拿錯不會動。
2. 給執行權限：`chmod +x ~/.claude/hooks/<檔名>`
3. 改 `~/.claude/settings.json` 做登記。寫法參考 `settings-example.json`。

**登記這一步最容易出事，注意兩件事：**

- 他的 `settings.json` 很可能已經有內容了。你要把新的項目**併進去**，不是整份覆蓋掉。覆蓋掉會弄壞他既有的設定。
- 改之前先備份一份，並且改完用 `python3 -c "import json;json.load(open('...'))"` 確認 JSON 格式沒壞。格式壞掉的話 Claude Code 會起不來，對非技術使用者是災難。

claim-guard 那兩支腳本要**一起裝**。只裝其中一支的話功能是失效的，而且不會有任何錯誤訊息，使用者會以為有保護但其實沒有。

### skills（五個流程腳本）

複製到 `~/.claude/skills/`，保留各自的資料夾。不需要登記，放進去就能用。

review-loop 帶了腳本與模板，整包一起複製，不要只搬 SKILL.md。裝完把 SKILL.md 裡「指令」那段的路徑改成他實際的安裝路徑，並且跑一次 `bash skills/review-loop/examples/regression/run-test.sh` 給他看（應該是通過 10、失敗 0），順便解釋那在測什麼。他如果沒有「反覆改版長文件」的需求，這個可以不裝。

checkpoint 跟 neat-freak 這兩個要照使用者自己的檔案擺法調整才會準。裝完之後問他平常筆記跟專案放在哪，然後幫他改 `skills/neat-freak/references/sync-matrix.md` 那張表。他答不出來就先跳過，那兩個 skill 沒調整過也不會壞，只是對帳結果會不準。

explain 跟 polite 直接可用，不用調整。

### 裝完一定要講的一句話

**「要把 Claude Code 完全關掉再重開，剛裝的東西才會生效。」**

這些攔截工具是啟動的時候才載入的。不重開等於沒裝，而使用者會以為裝好了。這句話一定要講，而且要講得夠明確。

## 使用者反悔的時候

有人裝完會覺得被擋得很煩，這很正常。要拿掉的話：

- 只想暫時停用某一個：去 `~/.claude/settings.json` 把那一段刪掉，重開就好。腳本檔可以留著。
- 想全部拿掉：刪掉 `settings.json` 裡相關的段落，再刪掉 `~/.claude/hooks/` 跟 `~/.claude/skills/` 底下對應的檔案。

不要因為他想拿掉就試圖說服他留下。這是他的電腦。

## 你自己要注意的

- **不要宣稱裝好了卻沒驗證。** 這整包東西的主題就是「不要相信沒有證據的完成宣稱」，你自己更不能犯。複製完要確認檔案真的在那裡，改完 JSON 要確認格式沒壞。
- **每個檔案在複製之前你自己先讀過。** 這是會攔截工具呼叫的程式，你在幫別人裝一個會影響他所有工作的東西。
- 出錯就照實講。裝到一半卡住，講清楚卡在哪，不要跳過去繼續裝下一個。
